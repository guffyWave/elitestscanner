import { COLORS } from './colors';
import { FONT_SIZES, FONT_WEIGHTS } from './fonts';
import { SIZES } from './dimens';

//@note - Note not all places theme have been used due to time constraints

export const theme = {
  colors: COLORS,
  fonts: {
    sizes: FONT_SIZES,
    weights: FONT_WEIGHTS,
  },
  dimensions: SIZES,
};

export default theme;